% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ldsc.R
\name{ldsc}
\alias{ldsc}
\title{Run LD Score Regression}
\usage{
ldsc(
  traits,
  sample.prev,
  population.prev,
  ld,
  wld,
  trait.names = NULL,
  sep_weights = FALSE,
  chr = 22,
  n.blocks = 200,
  ldsc.log = NULL,
  stand = FALSE,
  select = FALSE,
  chisq.max = NA,
  parallel = TRUE,
  cores = NULL
)
}
\arguments{
\item{traits}{Character vector of paths to .sumstats.gz files}

\item{sample.prev}{Numeric vector of sample prevalences (NA for continuous traits)}

\item{population.prev}{Numeric vector of population prevalences (NA for continuous)}

\item{ld}{Path to LD score directory (containing {chr}.l2.ldscore.gz files)}

\item{wld}{Path to weight LD score directory (defaults to \code{ld})}

\item{trait.names}{Character vector of trait names (defaults to V1, V2, ...)}

\item{sep_weights}{Use separate weight LD scores (ignored in gsemr)}

\item{chr}{Number of chromosomes (default 22)}

\item{n.blocks}{Number of jackknife blocks (default 200)}

\item{ldsc.log}{Log file path (ignored in gsemr)}

\item{stand}{Standardize output (default FALSE)}

\item{select}{Variable selection method (default FALSE)}

\item{chisq.max}{Maximum chi-square filter (default NA = auto)}

\item{parallel}{Use a parallel rayon worker pool for the per-pair
regression loop (default \code{TRUE}). Set to \code{FALSE} to force
single-threaded execution.}

\item{cores}{Integer cap on the rayon pool size. When \code{NULL}
(the default) rayon honours \code{RAYON_NUM_THREADS} if set, else
it uses the number of logical cores reported by the OS. On
many-core machines (32+) or when the underlying BLAS is
multithreaded, set this explicitly to avoid oversubscribing CPUs
with nested BLAS threads.}
}
\value{
A list with components:
  \item{S}{Genetic covariance matrix (k x k)}
  \item{V}{Sampling covariance matrix of S (k* x k*, where k* = k(k+1)/2)}
  \item{I}{LDSC intercept matrix (k x k)}
  \item{N}{Sample size vector}
  \item{m}{Number of SNPs used}
}
\description{
Estimates genetic covariance matrix (S), sampling covariance matrix (V),
and intercept matrix (I) from GWAS summary statistics using LD Score
Regression with block jackknife.
}
\examples{
\dontrun{
covstruc <- ldsc(
  traits = c("T1.sumstats.gz", "T2.sumstats.gz", "T3.sumstats.gz"),
  sample.prev = c(NA, NA, NA),
  population.prev = c(NA, NA, NA),
  ld = "eur_w_ld_chr/",
  wld = "eur_w_ld_chr/",
  trait.names = c("V1", "V2", "V3")
)
covstruc$S   # genetic covariance matrix
covstruc$I   # LDSC intercept matrix
}
}
