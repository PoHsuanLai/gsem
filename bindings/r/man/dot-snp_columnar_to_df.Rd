% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal_threads.R
\name{.snp_columnar_to_df}
\alias{.snp_columnar_to_df}
\title{Wrap a two-table GWAS result from the Rust binding into R data frames.}
\usage{
.snp_columnar_to_df(result)
}
\arguments{
\item{result}{Two-element list returned by the Rust binding.}
}
\value{
A two-element named list (`snps` + `params`, or `genes` +
  `params`), each slot being a `data.frame`.
}
\description{
The Rust side returns `list(snps = ..., params = ...)` (or `list(genes
= ..., params = ...)` for TWAS) where each inner slot is already a
columnar list of equal-length vectors. Use `structure(..., class =
"data.frame", row.names = .set_row_names(n))` to skip the
`data.frame()` constructor's per-column type coercion, which is
prohibitive at 20M-row params tables.
}
\keyword{internal}
