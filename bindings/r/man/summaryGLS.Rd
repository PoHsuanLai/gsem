% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaryGLS.R
\name{summaryGLS}
\alias{summaryGLS}
\title{Generalized Least Squares}
\usage{
summaryGLS(OBJECT = NULL, Y = NULL, V_Y = NULL, PREDICTORS, INTERCEPT = TRUE)
}
\arguments{
\item{OBJECT}{LDSC result object (used to extract Y and V_Y if provided)}

\item{Y}{Response vector (vech of genetic covariance matrix)}

\item{V_Y}{Covariance matrix of Y}

\item{PREDICTORS}{Predictor matrix (n x p)}

\item{INTERCEPT}{Include intercept (default TRUE)}
}
\value{
A numeric matrix with columns \code{betas, pvals, SE, Z},
  matching \code{GenomicSEM::summaryGLS}.
}
\description{
Runs GLS regression on genetic parameters. Drop-in replacement for
\code{GenomicSEM::summaryGLS()}: returns a numeric matrix with
columns \code{betas, pvals, SE, Z} and row names \code{b0, b1, ...}
(intercept first when \code{INTERCEPT = TRUE}), and prints it before
returning.
}
\examples{
# GLS regression of a synthetic response on two predictors.
Y <- c(0.50, 0.30, 0.20, 0.10)
V_Y <- diag(4) * 0.01
X <- matrix(c(1, 2, 3, 4,
              0, 1, 0, 1), nrow = 4, ncol = 2)
fit <- summaryGLS(Y = Y, V_Y = V_Y, PREDICTORS = X, INTERCEPT = TRUE)
fit   # matrix with rows b0, b1, b2 and columns betas, pvals, SE, Z
}
