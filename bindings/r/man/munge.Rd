% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/munge.R
\name{munge}
\alias{munge}
\title{Munge GWAS Summary Statistics}
\usage{
munge(
  files,
  hm3,
  trait.names = NULL,
  N = NULL,
  info.filter = 0.9,
  maf.filter = 0.01,
  log.name = NULL,
  column.names = list(),
  parallel = FALSE,
  cores = NULL,
  overwrite = TRUE
)
}
\arguments{
\item{files}{Character vector of GWAS file paths}

\item{hm3}{Path to HapMap3 SNP reference file}

\item{trait.names}{Character vector of trait names}

\item{N}{Optional sample-size override. Accepts \code{NULL} (infer
N from every file), a scalar (applied uniformly to all traits),
or a length-\code{length(files)} vector with one entry per trait
(use \code{NA} in any slot to infer N from that file). Matches
\code{GenomicSEM::munge}'s per-trait override semantics.}

\item{info.filter}{INFO score filter threshold (default 0.9)}

\item{maf.filter}{MAF filter threshold (default 0.01)}

\item{log.name}{Log file name (ignored in gsemr)}

\item{column.names}{Named list of column name overrides}

\item{parallel}{Accepted for API compatibility; gsemr's munge is currently
single-threaded so this has no effect.}

\item{cores}{Accepted for API compatibility; see \code{parallel}.}

\item{overwrite}{Overwrite existing files (ignored in gsemr)}
}
\value{
Character vector of output file paths (invisibly)
}
\description{
Filters and harmonizes GWAS summary statistics for use with
\code{\link{ldsc}}.
}
\examples{
\dontrun{
# Munge raw GWAS files against a HapMap3 reference SNP list.
munge(
  files = c("gwas1.txt", "gwas2.txt"),
  hm3 = "w_hm3.snplist",
  trait.names = c("T1", "T2")
)
# Writes T1.sumstats.gz, T2.sumstats.gz to the working directory.
}
}
