% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s_ldsc.R
\name{s_ldsc}
\alias{s_ldsc}
\title{Stratified LD Score Regression}
\usage{
s_ldsc(
  traits,
  sample.prev = NULL,
  population.prev = NULL,
  ld,
  wld,
  frq,
  trait.names = NULL,
  n.blocks = 200,
  ldsc.log = NULL,
  exclude_cont = TRUE
)
}
\arguments{
\item{traits}{Character vector of paths to .sumstats.gz files}

\item{sample.prev}{Numeric vector of sample prevalences (NULL for continuous traits)}

\item{population.prev}{Numeric vector of population prevalences (NULL for continuous)}

\item{ld}{Path to annotation LD score directory}

\item{wld}{Path to weight LD score directory}

\item{frq}{Path to frequency file directory}

\item{trait.names}{Character vector of trait names (defaults to V1, V2, ...)}

\item{n.blocks}{Number of jackknife blocks (default 200)}

\item{ldsc.log}{Log file path (ignored in gsemr)}

\item{exclude_cont}{Exclude continuous annotations (default TRUE)}
}
\value{
A list with per-annotation S and V matrices
}
\description{
Extends standard LDSC to multiple annotation categories, estimating
per-annotation contributions to genetic covariance and heritability.
}
\examples{
\dontrun{
# Stratified LDSC requires annotation-specific LD, weight, and freq files.
result <- s_ldsc(
  traits = c("T1.sumstats.gz", "T2.sumstats.gz"),
  ld = "baseline_LD/",
  wld = "weights/",
  frq = "frq/",
  trait.names = c("V1", "V2")
)
# Feed into `enrich()` for annotation enrichment tests.
}
}
