% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiGene.R
\name{multiGene}
\alias{multiGene}
\title{Multi-Gene Joint Analysis}
\usage{
multiGene(covstruc, Genes, LD, GeneSE = "F", Genelist = "F")
}
\arguments{
\item{covstruc}{LDSC result (named list with S, V, I, N, m components)}

\item{Genes}{Data frame of gene summary statistics (with beta, se, var columns per gene)}

\item{LD}{LD correlation matrix between genes}

\item{GeneSE}{Gene SE override (default "F" = auto, numeric = override)}

\item{Genelist}{Optional gene list to subset (character vector)}
}
\value{
A list with converged, chisq, df, and params
}
\description{
Fits a model with multiple genes simultaneously, accounting for LD between them.
Port of R GenomicSEM's \code{multiGene()}. Reuses the multi-SNP engine.
}
\examples{
\dontrun{
# `covstruc` from `ldsc()`, `Genes` from a TWAS-style per-gene table,
# and `LD` from the gene-level correlation matrix.
result <- multiGene(
  covstruc = covstruc,
  Genes = gene_df,     # data.frame with beta, se, var per gene
  LD = ld_matrix       # n_genes x n_genes correlation matrix
)
result$results
}
}
