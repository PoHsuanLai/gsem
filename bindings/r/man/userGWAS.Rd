% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/userGWAS.R
\name{userGWAS}
\alias{userGWAS}
\title{Run User-Specified GWAS}
\usage{
userGWAS(
  covstruc = NULL,
  SNPs = NULL,
  estimation = "DWLS",
  model = "",
  printwarn = TRUE,
  sub = FALSE,
  cores = NULL,
  toler = FALSE,
  SNPSE = FALSE,
  parallel = TRUE,
  GC = "standard",
  MPI = FALSE,
  smooth_check = FALSE,
  TWAS = FALSE,
  std.lv = FALSE,
  fix_measurement = TRUE,
  Q_SNP = FALSE
)
}
\arguments{
\item{covstruc}{LDSC result (named list with S, V, I, N, m components)}

\item{SNPs}{Path to merged summary statistics file}

\item{estimation}{Estimation method: "DWLS" (default) or "ML"}

\item{model}{lavaan-style model syntax}

\item{printwarn}{Print warnings (default TRUE; FALSE suppresses Rust warnings)}

\item{sub}{Subset of results to return (default FALSE = all)}

\item{cores}{Integer cap on the rayon worker pool size used for the
per-SNP fit loop. When \code{NULL} (the default) rayon honours
\code{RAYON_NUM_THREADS} if set, else it uses the number of
logical cores reported by the OS. On many-core machines (32+) or
when the underlying BLAS is multithreaded, set this explicitly to
avoid oversubscribing CPUs with nested BLAS threads.}

\item{toler}{Tolerance (accepted; convergence controlled by L-BFGS internally)}

\item{SNPSE}{SNP SE override (default FALSE = auto)}

\item{parallel}{Use a parallel rayon worker pool for the per-SNP
fit loop (default \code{TRUE}). Set to \code{FALSE} to force
single-threaded execution.}

\item{GC}{Genomic control: "standard" (default), "conservative", or "none"}

\item{MPI}{Use MPI (ignored in gsemr -- not applicable to Rust backend)}

\item{smooth_check}{Check for non-positive-definite matrices (default FALSE)}

\item{TWAS}{TWAS gene-level analysis mode (default FALSE)}

\item{std.lv}{Standardize latent variables (default FALSE)}

\item{fix_measurement}{Fix measurement model (default TRUE)}

\item{Q_SNP}{Compute Q_SNP heterogeneity statistic (default FALSE)}
}
\value{
A two-element list in normalized relational form:
  \describe{
    \item{\code{snps}}{One row per SNP with columns \code{SNP, CHR, BP,
      MAF, A1, A2, chisq, df, converged} (plus \code{Q_chisq, Q_df,
      Q_pval} when \code{Q_SNP = TRUE}).}
    \item{\code{params}}{Flat parameter table with columns \code{SNP,
      lhs, op, rhs, est, se, z, p}; join to \code{snps} on \code{SNP}.}
  }
  When \code{TWAS = TRUE} the first slot is named \code{genes} with
  columns \code{Gene, Panel, HSQ, chisq, df, converged}; the
  \code{params} table uses \code{Gene} in place of \code{SNP}.
}
\description{
Runs multivariate GWAS with a user-specified SEM model per SNP.
}
\note{
This replaces the earlier one-row-per-SNP-with-list-column
  shape; the old layout made R reconstruction O(N) `data.frame()`
  calls and hung at multi-million-SNP scale. Example filters:
  `result$params[result$params$SNP == "rsXXXX", ]` for one SNP, or
  `result$params[result$params$op == "~" & result$params$rhs == "SNP", ]`
  for every SNP's regression effect.
}
\examples{
\dontrun{
# `covstruc` from `ldsc()`, `merged_sumstats.tsv` from `sumstats()`.
result <- userGWAS(
  covstruc = covstruc,
  SNPs = "merged_sumstats.tsv",
  model = "F1 =~ NA*V1 + V2 + V3\nF1 ~ SNP\nF1 ~~ 1*F1"
)
# Per-SNP metadata:
head(result$snps)
# SNP regression effects (F1 ~ SNP) across all SNPs:
head(result$params[result$params$op == "~" & result$params$rhs == "SNP", ])
}
}
