% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rgmodel.R
\name{rgmodel}
\alias{rgmodel}
\title{Model-Implied Genetic Correlation Matrix}
\usage{
rgmodel(LDSCoutput, model, std.lv = TRUE, estimation = TRUE, sub = NULL, ...)
}
\arguments{
\item{LDSCoutput}{LDSC result (named list with S, V, I, N, m components)}

\item{model}{lavaan-style model syntax (optional, uses common factor if missing)}

\item{std.lv}{Standardize latent variables (default TRUE)}

\item{estimation}{Estimation method: TRUE means "DWLS", "ML" for ML}

\item{sub}{Subset of output (default NULL = all)}

\item{...}{Additional arguments (ignored)}
}
\value{
A list with components:
  \item{R}{Model-implied genetic correlation matrix}
  \item{V_R}{Sampling covariance of vech(R)}
}
\description{
Fits a common factor model and computes the model-implied genetic
correlation matrix R and its sampling covariance V_R.
}
\examples{
# Synthetic 3-trait covariance structure (normally from `ldsc()`).
covstruc <- list(
  S = matrix(c(0.60, 0.42, 0.35,
               0.42, 0.50, 0.30,
               0.35, 0.30, 0.40), 3, 3,
             dimnames = list(c("V1", "V2", "V3"), c("V1", "V2", "V3"))),
  V = diag(6) * 0.001,
  I = diag(3),
  N = c(1e5, 1e5, 1e5),
  m = 1e6
)
rg <- rgmodel(covstruc)
rg$R    # model-implied genetic correlation matrix
}
