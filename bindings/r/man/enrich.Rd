% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich.R
\name{enrich}
\alias{enrich}
\title{Enrichment Analysis}
\usage{
enrich(
  s_covstruc,
  model = "",
  params = NULL,
  fix = "regressions",
  std.lv = FALSE,
  rm_flank = TRUE,
  tau = FALSE,
  base = TRUE,
  toler = NULL,
  fixparam = NULL
)
}
\arguments{
\item{s_covstruc}{Stratified LDSC result (list with S_baseline, S_annot, V_annot, annotation_names, m_annot, m_total)}

\item{model}{lavaan-style model syntax (default "" = use basic proportional enrichment test)}

\item{params}{Character vector of parameter names to test (default NULL = all free params)}

\item{fix}{Which parameters to fix: "regressions" (default), "loadings", or "none"}

\item{std.lv}{Standardize latent variables (default FALSE)}

\item{rm_flank}{Remove flanking regions (default TRUE; implemented in Rust s_ldsc engine)}

\item{tau}{Use tau parameterization (default FALSE; not yet used)}

\item{base}{Include baseline (default TRUE)}

\item{toler}{Gradient tolerance for optimizer (default NULL = auto)}

\item{fixparam}{Named list of parameters to fix at specific values (default NULL)}
}
\value{
A data frame with enrichment results
}
\description{
Tests for annotation enrichment using stratified LDSC results.
}
\details{
When \code{model} is provided, fits a SEM per annotation using
\code{\link{usermodel}} and tests for parameter differences vs baseline.
When \code{model} is empty, uses the fast Rust proportional enrichment test.
}
\examples{
\dontrun{
# Fast proportional enrichment test (default, model = "").
s_covstruc <- s_ldsc(
  traits = c("T1.sumstats.gz", "T2.sumstats.gz"),
  ld = "baseline_LD/", wld = "weights/", frq = "frq/"
)
result <- enrich(s_covstruc)
head(result)
}
}
