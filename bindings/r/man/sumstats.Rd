% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sumstats.R
\name{sumstats}
\alias{sumstats}
\title{Merge GWAS Summary Statistics}
\usage{
sumstats(
  files,
  ref,
  trait.names = NULL,
  se.logit,
  OLS = NULL,
  linprob = NULL,
  N = NULL,
  betas = NULL,
  info.filter = 0.6,
  maf.filter = 0.01,
  keep.indel = FALSE,
  parallel = TRUE,
  cores = NULL,
  ambig = FALSE,
  direct.filter = FALSE,
  out = "merged_sumstats.tsv"
)
}
\arguments{
\item{files}{Character vector of GWAS file paths}

\item{ref}{Path to reference panel file (e.g., w_hm3.snplist)}

\item{trait.names}{Character vector of trait names}

\item{se.logit}{Logical vector indicating which traits have logistic SEs}

\item{OLS}{Logical vector indicating which traits are from OLS regression}

\item{linprob}{Logical vector indicating which traits are linear probability}

\item{N}{Numeric vector or list of sample size overrides}

\item{betas}{Named list of beta column overrides per trait (default NULL = auto-detect)}

\item{info.filter}{INFO score filter (default 0.6)}

\item{maf.filter}{MAF filter (default 0.01)}

\item{keep.indel}{Keep indels (default FALSE)}

\item{parallel}{Use a parallel rayon worker pool to read the
reference and GWAS files (default \code{TRUE}). Each input file is
decompressed and parsed on its own worker thread. Set to
\code{FALSE} to force single-threaded execution.}

\item{cores}{Integer cap on the rayon pool size. When \code{NULL}
(the default) rayon honours \code{RAYON_NUM_THREADS} if set, else
it uses the number of logical cores reported by the OS. Since the
reads are parallelized across files, values above
\code{length(files) + 1} don't help. On many-core machines (32+)
or when the underlying BLAS is multithreaded, set this explicitly
to avoid oversubscribing CPUs with nested BLAS threads.}

\item{ambig}{Keep ambiguous SNPs (default FALSE)}

\item{direct.filter}{Apply MAF filter directly to GWAS file frequencies (default FALSE)}
}
\value{
Path to the merged output file
}
\description{
Reads multiple GWAS files, applies QC filters, aligns alleles,
and outputs a merged tab-delimited file for multivariate GWAS.
}
\examples{
\dontrun{
# Merge munged sumstats into a single SNP x trait TSV for GWAS.
sumstats(
  files = c("T1.sumstats.gz", "T2.sumstats.gz"),
  ref = "eur_w_ld_chr/",
  trait.names = c("V1", "V2"),
  out = "merged_sumstats.tsv"
)
}
}
