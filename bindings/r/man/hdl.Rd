% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdl.R
\name{hdl}
\alias{hdl}
\title{High-Definition Likelihood Estimation}
\usage{
hdl(
  traits,
  sample.prev = NA,
  population.prev = NA,
  trait.names = NULL,
  LD.path,
  Nref = 335265,
  method = "piecewise"
)
}
\arguments{
\item{traits}{Character vector of paths to .sumstats.gz files}

\item{sample.prev}{Numeric vector of sample prevalences (NA for continuous traits)}

\item{population.prev}{Numeric vector of population prevalences (NA for continuous)}

\item{trait.names}{Character vector of trait names (defaults to V1, V2, ...)}

\item{LD.path}{Path to HDL LD reference panel directory (text format)}

\item{Nref}{Reference panel sample size (default 335265 for UKB)}

\item{method}{HDL method: "piecewise" (default) or "jackknife"}
}
\value{
A list with components:
  \item{S}{Genetic covariance matrix (k x k)}
  \item{V}{Sampling covariance matrix}
  \item{I}{Intercept matrix}
  \item{m}{Number of SNPs used}
}
\description{
Estimates genetic covariance using the HDL method, which uses
LD eigenvalue decomposition for more precise estimation than LDSC.
}
\examples{
\dontrun{
# HDL uses a text-format LD panel directory (see convert_hdl_panels()).
result <- hdl(
  traits = c("T1.sumstats.gz", "T2.sumstats.gz"),
  sample.prev = c(NA, NA),
  population.prev = c(NA, NA),
  LD.path = "hdl_text_panels/",
  trait.names = c("V1", "V2")
)
result$S
}
}
