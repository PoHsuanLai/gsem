% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usermodel.R
\name{usermodel}
\alias{usermodel}
\title{Fit a User-Specified SEM Model}
\usage{
usermodel(
  covstruc,
  estimation = "DWLS",
  model = "",
  CFIcalc = TRUE,
  std.lv = FALSE,
  imp_cov = FALSE,
  fix_resid = TRUE,
  toler = NULL,
  Q_Factor = FALSE
)
}
\arguments{
\item{covstruc}{Output from \code{\link{ldsc}}}

\item{estimation}{Estimation method: \code{"DWLS"} (default) or \code{"ML"}}

\item{model}{lavaan-style model syntax string}

\item{CFIcalc}{Compute CFI (default TRUE; FALSE skips null model fitting)}

\item{std.lv}{Standardize latent variables (default FALSE)}

\item{imp_cov}{Return model-implied covariance matrix (default FALSE)}

\item{fix_resid}{Fix residual variances to be positive (default TRUE)}

\item{toler}{Gradient norm tolerance for L-BFGS optimizer (default NULL = 1e-6)}

\item{Q_Factor}{Compute Q factor heterogeneity statistic (default FALSE)}
}
\value{
A list with components:
  \item{results}{Data frame of parameter estimates}
  \item{modelfit}{Data frame of fit indices (chisq, df, p_chisq, AIC, CFI, SRMR)}
  \item{converged}{Logical indicating convergence}
  \item{implied_cov}{Model-implied covariance matrix (if imp_cov=TRUE)}
  \item{Q_Factor}{Q factor results (if Q_Factor=TRUE)}
}
\description{
Fits a structural equation model to the genetic covariance structure
estimated by \code{\link{ldsc}}.
}
\examples{
# Synthetic 3-trait covariance structure (normally from `ldsc()`).
covstruc <- list(
  S = matrix(c(0.60, 0.42, 0.35,
               0.42, 0.50, 0.30,
               0.35, 0.30, 0.40), 3, 3,
             dimnames = list(c("V1", "V2", "V3"), c("V1", "V2", "V3"))),
  V = diag(6) * 0.001,
  I = diag(3),
  N = c(1e5, 1e5, 1e5),
  m = 1e6
)
um <- usermodel(
  covstruc,
  model = "F1 =~ NA*V1 + V2 + V3\nF1 ~~ 1*F1\nV1 ~~ V1\nV2 ~~ V2\nV3 ~~ V3"
)
um$results
um$modelfit
}
