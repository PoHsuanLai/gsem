% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiSNP.R
\name{multiSNP}
\alias{multiSNP}
\title{Multi-SNP Joint Analysis}
\usage{
multiSNP(covstruc, SNPs, LD, SNPSE = FALSE, SNPlist = NA)
}
\arguments{
\item{covstruc}{LDSC result (named list with S, V, I, N, m components)}

\item{SNPs}{Data frame of SNP summary statistics (with beta, se, var columns per SNP)}

\item{LD}{LD correlation matrix between SNPs}

\item{SNPSE}{SNP SE override (default FALSE = auto, numeric = override)}

\item{SNPlist}{Optional SNP list to subset (character vector)}
}
\value{
A list with converged, chisq, df, and params
}
\description{
Fits a model with multiple SNPs simultaneously, accounting for LD between them.
Port of R GenomicSEM's \code{multiSNP()}.
}
\examples{
\dontrun{
# `covstruc` from `ldsc()`, `SNPs` from `sumstats()` subset to the
# target SNPs, and `LD` from a reference panel (e.g. LDlinkR or PLINK).
result <- multiSNP(
  covstruc = covstruc,
  SNPs = snp_df,       # data.frame with beta, se, var per SNP
  LD = ld_matrix,      # n_snps x n_snps correlation matrix
  SNPSE = 0.0005
)
result$results
}
}
