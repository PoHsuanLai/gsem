LIBDIR = .
STATLIB = $(LIBDIR)/libgsemr.a

PKG_LIBS = -L$(LIBDIR) -lgsemr -lpthread -ldl -lm

all: C_clean

$(SHLIB): $(STATLIB)

C_clean:
	rm -Rf $(OBJECTS)

clean:
	rm -Rf $(STATLIB) $(OBJECTS) rust/target
